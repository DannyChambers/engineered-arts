import React from "react";

import { PageHeader } from "@dannychambers/milton-web-components";

const Header: React.FC = () => {
  return (
    <PageHeader>
      <></>
    </PageHeader>
  );
};

export default Header;
